# Placeholder for the with-secret fixture.
# The actual fake-credential string is materialised at TEST RUNTIME by the
# runner via printf concat (so secrets-guard does not flag the committed file).
# See tests/gdrive_import_integration.sh::materialise_with_secret_fixture().
